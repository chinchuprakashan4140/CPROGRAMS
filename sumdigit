#include<stdio.h>
void main()
{
int n,sum=0,d;
printf("Enter a number\n");
scanf("%d",&n);
do
{
d=n%10;
sum=sum+d;
n=n/10;
}
while(n>0);
printf("sum=%d\n",sum);
}

output

[chinchu@localhost Desktop]$ cc sumdigit.c
[chinchu@localhost Desktop]$ ./a.out
Enter a number
123
sum=6

